# [頁面/元件名稱]

## Meta

- Module: [module-name]
- 頁面類型：search-page / form-page / detail-page / modal
- 路由: `/[path]`
- 頁面檔名: `[Name]View.vue`（或對應框架）
- 狀態：draft / approved / implemented
- 建立：YYYY-MM-DD
- 更新：YYYY-MM-DD

## 版本歷程

| 版本 | 日期 | 變更 |
|------|------|------|
| v1.0 | YYYY-MM-DD | 初始建立 |

## 說明

[此頁面的目的和使用情境]

## 權限

| 操作 | Permission Code | 說明 |
|------|----------------|------|
| 檢視 | [VIEW_CODE] | [誰可以看] |
| 編輯 | [EDIT_CODE] | [誰可以改] |
| 刪除 | [DELETE_CODE] | [誰可以刪] |

## 頁面初始化

- **路由**：`/[path]`
- **權限檢查**：[PERMISSION_CODE]（無權限時導向 403 頁面）
- **並行載入 API**：
  - `GET /api/v1/xxx` — [用途]
  - `GET /api/v1/yyy` — [用途]

## 頁面結構

```
┌──────────────────────────────────┐
│ 頁面標題              [操作按鈕] │
├──────────────────────────────────┤
│ 篩選器區塊                       │
├──────────────────────────────────┤
│ 資料列表 / 表單 / 內容           │
├──────────────────────────────────┤
│ 分頁控制                         │
└──────────────────────────────────┘
```

---

## 篩選器（search-page 適用）

| 欄位名稱 | 欄位類型 | 資料來源 | 預設值 | 說明 |
|---------|---------|---------|--------|------|
| [關鍵字] | input-text | — | — | 模糊搜尋 |
| [狀態] | select | 固定選項 / API | 全部 | [選項列表] |
| [日期範圍] | date-range | — | — | 格式 YYYY-MM-DD |
| [分類] | select | `GET /api/v1/options?type=xxx` | — | |

> 欄位類型：input-text / select / radio-group / date-range / number-range / checkbox

## 表格欄位（search-page 適用）

| 欄位名稱 | 資料欄位 | 型別 | 寬度 | 可排序 | 說明 |
|---------|---------|------|------|--------|------|
| [編號] | id | text | 120px | Y | 唯一識別碼 |
| [名稱] | name | text | auto | Y | 主要顯示 |
| [狀態] | status | badge | 100px | Y | 顏色對應見下方 |
| [建立日期] | createdAt | datetime | 160px | Y | YYYY-MM-DD HH:mm |
| [操作] | — | actions | 120px | — | 操作按鈕 |

**狀態顏色對應：**

| 狀態值 | 顏色 | 說明 |
|--------|------|------|
| [啟用] | Primary（藍） | |
| [停用] | Warning（黃） | |
| [待處理] | Neutral（灰） | |

**資料處理模式：** Client-side（< 500 筆）/ Server-side（> 1000 筆）

**空狀態顯示：**
- 無查詢結果：「查無符合條件的資料」
- 未執行查詢：「請執行查詢顯示資料」

## 表單欄位（form-page 適用）

| 欄位名稱 | 欄位類型 | 必填 | 驗證規則 | 預設值 | 說明 |
|---------|---------|------|---------|--------|------|
| [欄位名] | input-text | Y | 長度 2-50 | — | |
| [選擇欄位] | select | Y | 必須選擇 | — | 選項來源 API |
| [日期] | date-picker | N | 不可選過去 | 今日 | YYYY-MM-DD |
| [檔案] | file-upload | N | PDF, ≤ 5MB | — | 支援多檔 |

## 操作按鈕

### 頁面層級按鈕

| 按鈕 | 類型 | 位置 | 權限 | 顯示條件 | 說明 |
|------|------|------|------|---------|------|
| [新增] | primary | 右上 | [CREATE_CODE] | 永遠 | 跳轉新增頁 |
| [匯出] | default | 右上 | [EXPORT_CODE] | 永遠 | 匯出 Excel |
| [批次刪除] | danger | 右上 | [DELETE_CODE] | 有勾選時 | 刪除勾選項 |

### 列表項目按鈕

| 操作 | 類型 | 顯示條件 | 說明 |
|------|------|---------|------|
| [檢視] | default | 所有項目 | 跳轉詳情頁 |
| [編輯] | primary | status = [可編輯] | 跳轉編輯頁 |
| [刪除] | danger | status = [可刪除] | 確認後刪除 |
| [啟用/停用] | warning | 有狀態控制 | 切換狀態 |

## 互動行為

### [操作名稱]

- **觸發條件**：[使用者動作]
- **確認彈窗**：[標題]「[確認訊息]」
- **呼叫 API**：`[METHOD] [PATH]`
- **Request Body**：`{ field: value }`
- **成功**：[關閉彈窗 / Toast 訊息 / 重新查詢 / 跳轉]
- **失敗**：[錯誤訊息顯示方式，彈窗保持/關閉]

## 狀態管理

| 狀態 | 初始值 | 變更時機 | 影響 |
|------|--------|---------|------|

## 驗證規則（form-page 適用）

| 欄位 | 規則 | 驗證時機 | 錯誤訊息 |
|------|------|---------|---------|

> 驗證時機：blur（失去焦點）/ submit（提交時）/ realtime（即時）

## 響應式設計

| 斷點 | 佈局變化 |
|------|---------|

## API 清單

本頁面涉及的所有 API：

| # | API | Method | Path | 用途 |
|---|-----|--------|------|------|
| 1 | [查詢列表] | GET | `/api/v1/xxx` | 載入列表資料 |
| 2 | [刪除] | DELETE | `/api/v1/xxx/:id` | 刪除單筆 |
| 3 | [更新狀態] | PUT | `/api/v1/xxx/:id/status` | 啟用/停用 |
| 4 | [載入選項] | GET | `/api/v1/options?type=xxx` | 篩選器選項 |

## 備註

- [特殊需求或限制]
